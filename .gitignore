# -----------------------------------------------------------------------------
# Listyyy – root .gitignore (backend/, frontend/, frontend/android/ have their own)
# -----------------------------------------------------------------------------

# Logs (root-level backend/frontend logs)
*.log
backend.log
logs/

# OS
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment and secrets (e.g. docker-compose .env)
.env
.env.*
!.env.example

# Release config (per-machine image name; copy from release.config.example)
release.config

# IDE / editor (project-specific IDE config lives in subprojects)
.idea/
.vscode/
*.swp
*.swo
*~

# Temporary and backup
*.tmp
*.temp
*.bak
*.backup
.cache/

# Backend static (frontend build copied for packaging; generated by scripts/package-for-windows.sh)
backend/src/main/resources/static/

# Windows package (generated by scripts/package-for-windows.sh)
listyyy-windows/
listyyy-windows.zip

# Optional: uncomment if you don't want to track Cursor project skills
# .cursor/

# Uploaded files (runtime data, not source)
backend/uploads/

# Keys and secrets
*.pem
*.keystore
*.jks
keystore/