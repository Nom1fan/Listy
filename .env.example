# ── EC2 Deployment ───────────────────────────────────────────
# Path to your SSH private key (.pem file)
EC2_PEM=/path/to/your-key.pem

# EC2 public hostname or IP address
EC2_HOST=your-ec2-public-dns-or-ip

# SSH user (default: ubuntu)
# EC2_USER=ubuntu

# Remote deployment directory (use full path, not ~)
# EC2_DEPLOY_DIR=/home/ubuntu/listyyy

# ── Docker Compose (used locally and on EC2) ─────────────────
JWT_SECRET=change-me-in-production-min-256-bits

# ── Twilio SMS OTP ───────────────────────────────────────────
# Sign up at https://www.twilio.com, then copy from Console Dashboard
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Buy a number or use the trial number (e.g. +1234567890)
TWILIO_FROM_NUMBER=

# ── Gmail SMTP (Email OTP) ──────────────────────────────────
# 1. Enable 2-Step Verification on your Google Account
# 2. Create an App Password at https://myaccount.google.com/apppasswords
MAIL_USERNAME=
MAIL_PASSWORD=

# ── Image Search APIs ───────────────────────────────────────
# GIPHY Stickers – get key at https://developers.giphy.com/dashboard/
GIPHY_API_KEY=
# Pixabay – get key at https://pixabay.com/api/docs/
PIXABAY_API_KEY=

# ── Database (production) ───────────────────────────────────
# Set a strong password for production PostgreSQL
DB_PASSWORD=change-me-in-production
